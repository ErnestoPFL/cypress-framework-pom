# Cypress Framework with POM pattern

* Install node dependencies `yarn install & npm install`

* Rename .env.example to .env

To run tests with cypress app run `yarn run cy:open`

## Run tests in headless mode

`yarn run cy:run`

